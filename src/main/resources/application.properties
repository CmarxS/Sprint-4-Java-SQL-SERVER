# App
spring.application.name=Motomap

# SQL Server
spring.datasource.url=jdbc:sqlserver://sql-server-cp3-rm555997-eastus2.database.windows.net:1433;database=financeiro_db;encrypt=true;trustServerCertificate=true;loginTimeout=30;
spring.datasource.username=user-cp3
spring.datasource.password=Fiap@2tdsvms
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=0

# Thymeleaf (dev)
spring.thymeleaf.cache=false
